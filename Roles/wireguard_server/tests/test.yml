---
  - name: Connect to server and execute shell command
    hosts: wireguard
    gather_facts: false
     tasks:
     - name: Execute shell command
       shell: wg show wg0 peers
       register: command_output
       
    - name: Print command output
       debug:
       var: command_output.stdout_lines